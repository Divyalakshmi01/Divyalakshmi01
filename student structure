#include<stdio.h>
int main()
{
    struct student
    {
        int sno;
        char sname[20];
        int smarks;
    };
   struct student s1={101,"divya",81};
   printf("the sno is %d",s1.sno);
   printf("the sname is %s",s1.sname);
   printf("the smarks is %d",s1.smarks);
   return 0;
}
